#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    double n, π = 3.141592653;
    cin >> n;
    cout << fixed << setprecision(9) << π * n * n << endl;
    return 0;
}